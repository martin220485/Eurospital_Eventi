# Plan F8 — AD/LDAP

Branch `f8-ad-sso`. TDD. Spec: [F8 design](../specs/2026-05-28-f8-ad-sso-design.md).

---

## A — Backend

### A1. Migrazione 0010 + modifica User
- [ ] Test `test_migration.py`: assert colonne `auth_source`, `ldap_dn`, `department`, `ldap_groups`; perm `users.ldap_sync` su super_admin.
- [ ] `app/models/user.py` aggiungi 4 colonne.
- [ ] `alembic/versions/0010_ldap_users.py` (down `0009_reports_permission`): ALTER users add columns + seed perm.
- [ ] Test verde. Commit `feat(f8): users ldap fields + permesso users.ldap_sync`.

### A2. `ldap_service`
- [ ] Test `test_ldap_service.py` con `ldap3.Server(get_info=ALL, mode='MOCK_SYNC')` (in-memory mock): bind ok/fail; search user attrs default mapping; search groups (memberOf parsing CN=...,OU=...); `sync_user` create + update + role assignment.
- [ ] `app/services/ldap_service.py`:
  - `_connect(settings)` (LDAPS / startTLS, bind con bind_dn+pw)
  - `bind_user(db, username, password)` → bool (bind temp con `user_dn`)
  - `find_user(db, username)` → `{dn, attrs: {...}, groups: [...]}` o None
  - `sync_user(db, username)` → User (create/update + role)
  - `sync_users_in_group(db, group_cn)` → `{created, updated, errors}`
  - default mapping: `{"username":"sAMAccountName","email":"mail","full_name":"displayName","department":"department","groups":"memberOf"}`
- [ ] Test verde. Commit `feat(f8): ldap_service (bind+search+sync)`.

### A3. Modifica `auth_service.login`
- [ ] Test `test_ldap_login.py`: mock `ldap_service.bind_user` e `sync_user`. Casi: sso_enabled=True + bind ok → user sync'd; sso_enabled=True + bind fail + user is local → fallback password; sso_enabled=False → solo locale.
- [ ] Modifica `auth_service.login` come da spec §4.
- [ ] Test verde. Commit `feat(f8): auth_service prova LDAP prima del locale quando SSO attivo`.

### A4. Catalog visibility con gruppi AD
- [ ] Test `test_catalog_visibility_ldap.py`: seed evento `restricted` su group "IT"; user ldap con `ldap_groups=["IT"]` → vede; user ldap con `ldap_groups=["HR"]` → non vede; user local → non vede (back-compat F5).
- [ ] Modifica `catalog_service.list_visible_events` (o helper interno) per il branch ldap.
- [ ] Test verde + suite F5 catalog ancora verde. Commit `feat(f8): catalog mostra eventi ristretti agli utenti AD del gruppo target`.

### A5. Router `/api/admin/ldap/*`
- [ ] Schemas `app/schemas/ldap.py`: `LdapSettingsOut`, `LdapSettingsIn`, `LdapPreviewOut`, `LdapSyncResult`.
- [ ] Test `test_ldap_api.py`: 403 senza perm; settings get/put (password mai esposta); preview ritorna attrs+groups+roles; sync-user/sync-all chiamano i service helpers (mock).
- [ ] `app/api/routers/ldap.py` con i 5 endpoint dietro `require_permission("users.ldap_sync")`.
- [ ] Include in `main.py`.
- [ ] Test verde. Commit `feat(f8): API admin LDAP (settings/preview/sync)`.

---

## B — Frontend

### B1. Client API
- [ ] `frontend/lib/ldap-api.ts`: `getSettings/saveSettings/testConnection/preview/syncUser/syncAll`.

### B2. Pagina `/admin/settings/ldap`
- [ ] `components/admin/ldap/ldap-config-form.tsx`: form `sso_enabled`, `server_uri`, `base_dn`, `bind_dn`, `bind_password` (placeholder `(invariato)`), `user_filter`, `group_filter`, `attr_mapping` (textarea JSON), `users_group`, `admins_group`. Pulsanti "Salva", "Test connessione".
- [ ] `components/admin/ldap/sync-panel.tsx`: input username + "Anteprima", "Sync utente"; pulsante "Sync tutti gli utenti".
- [ ] `app/admin/settings/ldap/page.tsx` server component che carica le settings + monta i due componenti client.
- [ ] Aggancio sidebar: link "Impostazioni AD/LDAP".
- [ ] Test `__tests__/ldap-config-form.test.tsx`: render fields + submit.
- [ ] `pnpm test` + `pnpm build` verdi. Commit `feat(f8): admin UI settings LDAP + sync`.

---

## C — Docs

- [ ] `INSTALL.md` sezione "AD/LDAP (F8)":
  - `/admin/settings/ldap` per configurare e testare AD.
  - Toggle `sso_enabled` attiva login AD su `/login`.
  - Default mapping AD; override via JSON.
  - Sync su login + manuale.
  - Visibility eventi ristretti per gruppi/reparto.
  - Permesso `users.ldap_sync`.
- Commit `docs(f8): istruzioni AD/LDAP`.

---

## Self-Review

- §3 contratti API → A5; §4 login flow → A3; §5 visibility → A4; §6 test → tutti.
- Reuse: `ldap_settings` da F2, `event_visibility` da F3, `users` da F1.
- Out-of-scope rispettato: niente OIDC/SAML/beat/foto.
- Sicurezza: bind pw cifrata Fernet (già F2); password utente in chiaro mai persistita; timeout LDAP fissato.
- Backwards compat: utenti locali continuano a funzionare; visibility F5 invariata per utenti locali.
